{
  "name": "vlu-qe-pipeline-master",
  "version": "1.0.0",
  "description": "CI/CD pipeline with Smoke Test and Regression Test",
  "main": "auth.js",
  "scripts": {
    "test:smoke": "jest auth.smoke.test.js --runInBand",
    "test:regression": "jest auth.smoke.test.js auth.regression.test.js --runInBand"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  }
}
